# .claude/scripts/

User-owned scripts for this project — for example, executables referenced
by hooks configured in `.claude/settings.json`. Files here are not managed
by `punk-ai` and are safe to edit or add to freely.
